import { AppendArgPipe } from './append-arg.pipe';

describe('AppendArgPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendArgPipe();
    expect(pipe).toBeTruthy();
  });
});
